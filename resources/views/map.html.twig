{% extends 'layout.html.twig' %}
{% set active = 'map' %}

{% block head %}
    {{ map.headerjs|raw }}
{% endblock %}

{% block content %}
    <div class="map-form-widget">
        {{ form_widget(form) }}
    </div>
    {{ map.js|raw }}
    {{ map.body|raw }}
{% endblock %}
